% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dynamics.R
\name{success_bias_select_teacher}
\alias{success_bias_select_teacher}
\title{Success-biased teacher selection}
\usage{
success_bias_select_teacher(learner, model)
}
\arguments{
\item{learner}{An Agent instance evaluating neighbors.}

\item{model}{An AgentBasedModel instance (not used directly here, but included for consistency with other learning functions).}
}
\value{
An Agent object: the selected teacher.
}
\description{
Selects the most successful neighbor (highest fitness) to learn from. Ties are broken at random.
}
\examples{
agents <- c(Agent$new(id = 1), Agent$new(id = 2), Agent$new(id = 3))
graph <- igraph::make_graph(~ 1-2, 1-3)
model <- make_abm(graph = graph)
learner <- model$get_agent(1)
teacher <- success_bias_select_teacher(learner, model)
}
